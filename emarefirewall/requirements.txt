# EmareFirewall bağımlılıkları
# Core (manager.py, cli.py) — ek bağımlılık gerektirmez

# SSH bağlantısı için (uzak sunucu yönetimi)
paramiko>=2.12.0

# Web API kullanacaksanız (opsiyonel)
flask>=2.3.0

# ISP ölçek (EMARE_MODE=isp) — opsiyonel
# redis>=4.5.0
# psycopg2-binary>=2.9.0
# gunicorn>=21.2.0
# gevent>=23.9.0
